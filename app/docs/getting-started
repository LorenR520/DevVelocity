// app/docs/getting-started/layout.tsx

import DocsSidebar from "../../../components/DocsSidebar";
import MobileSidebar from "../../../components/MobileSidebar";

export const metadata = {
  title: "Getting Started – DevVelocity Docs",
  description:
    "Learn how to install, configure, and deploy DevVelocity optimized cloud images across AWS, Azure, GCP, OCI, DigitalOcean, Linode, and Vultr.",
};

export default function DocsLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex min-h-screen">

      {/* SIDEBAR (Desktop) */}
      <aside className="hidden md:block w-64 border-r border-gray-200 dark:border-neutral-800">
        <DocsSidebar />
      </aside>

      {/* MOBILE SIDEBAR */}
      <div className="md:hidden p-4 border-b border-gray-200 dark:border-neutral-800 flex items-center justify-between w-full">
        <MobileSidebar />
        <h1 className="text-lg font-semibold">Docs</h1>
      </div>

      {/* MAIN CONTENT */}
      <main className="flex-1 p-6">
        {children}
      </main>
    </div>
  );
}
