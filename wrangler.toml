name = "develocity"
compatibility_date = "2023-10-01"
pages_build_output_dir = ".vercel/output/static"

# Environment variables
[vars]
NODE_ENV = "production"

# Observability logs (correct structure)
[observability]
logs = { enabled = true }

# Optional: Logpush (Cloudflare Analytics)
[logpush]
enabled = false
